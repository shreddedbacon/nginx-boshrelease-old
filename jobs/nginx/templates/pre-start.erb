#!/bin/bash -ex
NGINX_DIR=<%= p('nginx_docroot') %>
#if [ ! -d $NGINX_DIR ]; then
#  mkdir -p $NGINX_DIR
#  cd $NGINX_DIR
#  echo "It verks!" > index.html
chown -R vcap:vcap $NGINX_DIR
#fi
#cd $NGINX_DIR
#echo "It verks forever once more" > index.html

